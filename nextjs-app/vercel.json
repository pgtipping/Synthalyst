{
  "version": 2,
  "buildCommand": "prisma generate && node scripts/handle-db-build.js && next build",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["iad1"],
  "env": {
    "NEXT_PUBLIC_VERCEL_ENV": "production"
  },
  "outputDirectory": ".next"
}
