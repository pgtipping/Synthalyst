"use client";

import { useEffect } from "react";
import "./globals.css";

export default function GlobalStyles() {
  // This component doesn't render anything, it just imports the global CSS
  return null;
}
